/**
 * Public API of the `shared/ui` segment.
 *
 * `shared` has no slices — it holds no business meaning, so there is nothing
 * to slice it by. It is organised only by segment: `ui`, `config`, and later
 * `lib` and `api` if they earn their place.
 */
export { Avatar } from "./Avatar";
export { EmptyState } from "./EmptyState";
export * from "./icons";
